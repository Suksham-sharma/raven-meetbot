pytest>=8.0

# Answer-quality eval (Ragas). NOTE: ragas hard-imports a langchain_community
# module path that langchain removed in 0.4.x, so a plain `pip install ragas`
# crashes on import against the current ecosystem — pin langchain to 0.3.x.
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
ragas==0.2.15
langchain<0.4
langchain-community<0.4
langchain-openai<0.3

# run_eval.py talks to /ask over stdlib urllib (no requests dep needed).
