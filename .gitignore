node_modules/
dist/
.env
.env.*
!.env.example
recordings/
.DS_Store
*.log
CLAUDE.md
bot/.auth/
.gstack/
# Per-machine port remaps and service overrides.
docker-compose.override.yml
# Local run output from scripts/demo.sh — service logs and a session cookie.
.demo-logs/
# Codex session handle, local to one machine.
.context/
